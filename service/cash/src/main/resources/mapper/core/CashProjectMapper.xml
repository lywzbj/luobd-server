<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luobd.server.cash.core.mapper.CashProjectMapper">

    <select id="page" resultType="com.luobd.server.cash.core.entity.CashProject">
        select * from cash_project where deleted = 0
        <if test="input.projectName != null and input.projectName.length > 0">
            and projectName like concat('%',#{input.projectName},'%')
        </if>
        <if test="input.startProjectDate != null">
            and projectDate >= #{input.startProjectDate}
        </if>
        <if test="input.endProjectDate != null">
            and projectDate &lt;= #{input.endProjectDate}
        </if>
        order by  createTime desc
    </select>

</mapper>
